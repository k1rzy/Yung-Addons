package com.encodey.YungAddons.Sounds;

import net.minecraftforge.client.event.sound.SoundEvent;
import scala.tools.nsc.transform.SpecializeTypes;

import java.awt.*;

public class soundshandler extends ArrayStoreException {
    public static void RenderArrayLocation(double arrayString) {
        if(arrayString <= 0) return;
        else
            arrayString = 1;
    }
}
